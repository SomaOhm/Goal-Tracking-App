# ── Backend (Python/FastAPI) ──
# Use postgresql+psycopg:// (psycopg3 driver — psycopg2 is NOT installed)
# Get from: Supabase dashboard → Settings → Database → Connection string (URI mode)
DATABASE_URL=postgresql+psycopg://postgres.[project-ref]:[password]@db.[project-ref].supabase.co:5432/postgres
GEMINI_API_KEY=placeholder
CELERY_BROKER_URL=placeholder

# ── Supabase REST client (used by supabase-py for the Postgres → Snowflake sync) ──
# Get from: Supabase dashboard → Settings → API
SUPABASE_URL=https://[project-ref].supabase.co
SUPABASE_ANON_KEY=placeholder

# ── Snowflake (Actual login credentials) ──
SNOWFLAKE_ACCOUNT=placeholder
SNOWFLAKE_USER=placeholder
SNOWFLAKE_PASSWORD=placeholder
SNOWFLAKE_DATABASE=placeholder
SNOWFLAKE_SCHEMA=placeholder
SNOWFLAKE_WAREHOUSE=placeholder

